services:
  - type: web
    name: whatsapp-userbot
    env: docker
    plan: starter
    autoDeploy: true
    dockerfilePath: Dockerfile
    buildCommand: ""        
    startCommand: "node index.js"
    envVars:
      - key: PORT
        value: "10000"
